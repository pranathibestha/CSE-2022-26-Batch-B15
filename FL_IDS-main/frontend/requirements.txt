# Frontend Desktop Application Dependencies
# These are Python dependencies that might be needed for the desktop app

# Core dependencies
fastapi==0.104.1
uvicorn==0.24.0
numpy==1.24.3
requests==2.32.4
websockets==12.0
python-dotenv==1.0.1

# Optional: For enhanced desktop features
pywin32>=306; sys_platform == "win32"
psutil==6.1.0

# Backend dependencies for desktop mode
scapy==2.7.0rc1
flwr==1.8.0
